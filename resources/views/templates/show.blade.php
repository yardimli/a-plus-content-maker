@extends('layouts.public')
@section('title', $template->name.' · Templates')
@section('content')
<section class="template-detail"><a href="{{ route('templates.index') }}" class="auth-back template-detail-back">← Back to gallery</a><div class="template-detail-copy"><span class="pill">{{ $template->category }}</span><h1>{{ $template->name }}</h1><p>{{ $template->description ?: $template->summary }}</p><div class="template-facts"><span><strong>{{ $template->modules->count() }}</strong> modules</span><span><strong>{{ in_array('Series', $template->tags ?? []) ? '3+' : '1' }}</strong> book layout</span></div>@auth<a href="{{ route('projects.create', ['template' => $template->slug]) }}" class="button button-primary button-large">Clone this template</a>@else<a href="{{ route('register') }}" class="button button-primary button-large">Create account to use</a>@endauth</div><div><img class="template-detail-image" src="{{ $template->preview_image }}" alt="{{ $template->name }} publishing campaign"><div class="template-sequence"><p class="eyebrow">Page sequence</p>@foreach($template->modules as $module)<div class="sequence-module"><span>{{ str_pad($loop->iteration, 2, '0', STR_PAD_LEFT) }}</span><div><strong>{{ config('modules.'.$module->module_type.'.name') }}</strong><small>{{ config('modules.'.$module->module_type.'.description') }}</small></div></div>@endforeach</div></div></section>
@endsection
