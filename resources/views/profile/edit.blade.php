@extends('layouts.app')
@section('title', 'Profile') @section('page-title', 'Account settings')
@section('content')
<div class="settings-grid"><section class="form-card"><p class="eyebrow">Profile</p><h2>Your studio identity</h2><form method="POST" action="{{ route('profile.update') }}" class="form-stack">@csrf @method('PATCH')<label>Name<input name="name" value="{{ old('name', $user->name) }}" required>@error('name')<span class="field-error">{{ $message }}</span>@enderror</label><label>Email<input type="email" name="email" value="{{ old('email', $user->email) }}" required>@error('email')<span class="field-error">{{ $message }}</span>@enderror</label><button class="button button-primary" type="submit">Save profile</button></form></section><section class="form-card"><p class="eyebrow">Security</p><h2>Change password</h2><form method="POST" action="{{ route('password.update') }}" class="form-stack">@csrf @method('PUT')<label>Current password<input type="password" name="current_password" autocomplete="current-password"></label><label>New password<input type="password" name="password" autocomplete="new-password"></label><label>Confirm password<input type="password" name="password_confirmation" autocomplete="new-password"></label><button class="button button-secondary" type="submit">Update password</button></form></section></div>
@endsection
