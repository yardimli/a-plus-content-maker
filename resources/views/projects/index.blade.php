@extends('layouts.app')
@section('title', 'My projects') @section('page-title', 'My A+ projects')
@section('content')
<div class="page-lead"><div><p class="eyebrow">Your working library</p><h2>Every book page, in one place.</h2><p>Open a draft, review its modules, or begin something new.</p></div><a href="{{ route('projects.create') }}" class="button button-primary">＋ New project</a></div>
<div class="project-grid">@forelse($projects as $project)<article class="project-card"><div class="project-cover"><span>{{ $project->asin ?: 'DRAFT' }}</span><strong>{{ $project->name }}</strong><small>{{ $project->genre ?: 'A+ CONTENT' }}</small></div><div class="project-card-body"><div class="card-meta"><span>{{ $project->modules_count }} modules</span><span>{{ ucfirst($project->status) }}</span></div><h3>{{ $project->name }}</h3><p>Updated {{ $project->updated_at->diffForHumans() }}</p><div class="card-actions"><a href="{{ route('projects.builder', $project) }}" class="button button-secondary">Open builder</a><form method="POST" action="{{ route('projects.destroy', $project) }}" data-confirm="Delete this project and all its modules and images?">@csrf @method('DELETE')<button class="text-button danger" type="submit">Delete</button></form></div></div></article>@empty<div class="empty-card"><h3>No projects yet</h3><p>Create your first A+ content page.</p><a href="{{ route('projects.create') }}" class="button button-primary">Get started</a></div>@endforelse</div>{{ $projects->links() }}
@endsection
